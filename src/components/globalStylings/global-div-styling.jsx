import styled from "styled-components";

//this is used to display blocks in a new line, when window is small
export const GlobalDivSeperator = styled.div`
  display: block;
  float: left;
  flex-direction: row;
`;
